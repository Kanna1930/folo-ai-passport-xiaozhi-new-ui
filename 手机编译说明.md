# 手机编译说明（无需电脑）

这份源码已经加入 CW2017 电量计读取和屏幕数字电量显示。

## 最简单的方法

1. 在手机浏览器打开 GitHub，并登录自己的账号。
2. 新建一个空的 **Public repository**（例如 `folo-passport-battery`）。
3. 把这个压缩包里的全部文件上传到仓库根目录，注意不要再套一层 `folo-ai-passport-xiaozhi-main` 文件夹。
4. 打开仓库顶部的 **Actions**。
5. 选择 **Build Folo AI Passport (Battery Display)**。
6. 点击 **Run workflow**，等待构建完成。
7. 构建成功后打开本次运行，在 **Artifacts** 中下载 `folo-ai-passport-battery-display`。
8. 解压后得到 `merged-binary.bin`，这就是完整 8MB 固件镜像。

## 注意

- 本工程针对 FoloToy AI Passport / ESP32-C3 / 8MB Flash。
- 本版本固定使用 ESP-IDF 5.5.3，便于与官方已经验证的环境保持一致。
- 源码层面已经加入 CW2017（I2C 地址 0x63）和 520mAh 电池 profile。
- 屏幕顶部会在电池图标旁显示类似 `85%` 的数字。
- 该硬件版本没有独立的充电状态输入，因此只显示电量百分比，不显示“充电中”闪电标记。
- 本次源码无法在当前环境直接完成 ESP-IDF 固件编译，所以 GitHub Actions 构建成功才算得到可刷写的 BIN；刷入真机后的实际电量读数仍需实机验证。
